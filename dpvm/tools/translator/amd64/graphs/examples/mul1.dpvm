/* multiply two long numbers of size 1 qwords; generated by gen_mul_long(), T17.550-T17.550 (dimorphic_supercilious_capital) */

int z = 0x8000000000000000;

(int x0, int y0) ->
(int z1, int z0) {
	int z0 = z;

	{
		int p;
		{ int l = x0; int h = y0; asm { mul }; l += z0; int r = l < z0; z0 = p + l; l = z0 < l; p = l + h + r; }
		y0 = p + z;
	}

	z0 += z;
	y0 += z;

	return (y0, z0);
}
