/* translator, registry class; T19.643-T19.643; $DVS:time$ */

#include "../registry/registry.dpvmake"

(const stateful any regSrc) -> (volatile registryData reg, const char error[]) registryPrepare = {
	registryData reg = registryObj.copy(regSrc);
	char error[] = registryObj.check(reg, REGISTRY_CHECKER);
	return (reg, error);
};
