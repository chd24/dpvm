/* debugger class, dump demo, T19.645-T19.645; $DVS:time$ */
/* Generated by Class source generator, ver 0.1.0, T19.354-T19.358 */

#include "../debugger.dpvmake"

(int flags) -> (const char error[]) dump = {
	debuggerClass debuggerObj = debuggerCreate();

	char error[] = debuggerObj.methods.setFlags(debuggerObj, flags);
	if (error.csize) return error;

	char out[];
	error = debuggerObj.methods.dump(out, debuggerObj);
	if (error.csize) return error;

	outputp("", "", out);
	return "";
};