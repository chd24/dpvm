/* json class, dump demo, T19.555-T19.555; $DVS:time$ */
/* Generated by Class source generator, ver 0.1.0, T19.354-T19.358 */

#include "../json.dpvmake"

(int flags) -> (const char error[]) dump = {
	jsonClass jsonObj = jsonCreate();

	char error[] = jsonObj.methods.setFlags(jsonObj, flags);
	if (error.csize) return error;

	char out[];
	error = jsonObj.methods.dump(out, jsonObj);
	if (error.csize) return error;

	outputp("", "", out);
	return "";
};