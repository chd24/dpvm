/* view entry by link E, T18.327-T18.334; $DVS:time$ */

#include "../internal.dpvmh"

(volatile stateful teData data) -> (const char result[]) commandE {
	int index0 = data.pos / 36 + 3;
	int index;
	for (index = data.nextIndexes[index0]; index != index0 && index & 3; index = data.nextIndexes[index]);
	if (!(index & 3))
		data.pos = index * 36;

	return "view";
}

teCommand cmdE = {
        "E",
        "view entry referred by field E of current entry",
        commandE
};


