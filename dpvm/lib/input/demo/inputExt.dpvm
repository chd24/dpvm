/* input class, inputExt demo, T20.229-T20.329; $DVS:time$ */

#include "demo.dpvmake"

(const any callback, volatile stateful any data, int maxSize, int timeout) -> () inputExt = {
	return editLine(callback, data, "", maxSize, timeout);
};
