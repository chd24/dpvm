/* find c in str[begin..end), return index or -1; T15.554-T16.598; $DVS:time$ */
	
(const stateful char str[], int begin, int end, char c) -> (int index) {
	int i;

	for (i = begin; i < end; i = i + 1)
		if (str[i] == c) return i;

	return -1;
}
