/* find c in str[begin..end) backward, return index or -1; T15.554-T20.331; $DVS:time$ */
	
(const stateful char str[], int begin, int end, char c) -> (int index) substrrchr = {
	int i;

	for (i = end - 1; i >= begin; i--)
		if (str[i] == c) return i;

	return -1;
};
